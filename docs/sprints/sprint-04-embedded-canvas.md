# Sprint 04 - Embedded DagEdit Canvas

## Goal

Place a real `DagEdit` editor surface inside the center document host of `cstudio`.

## Scope

- evolve document tabs so they can host either text summaries or live Avalonia content
- add a `DagEditDocumentView` inside the adapter layer
- seed the embedded editor from the adapter sample graph
- keep the shell shape aligned with the `GPU-Reshape Studio` reference while replacing the mock center text panel with a real canvas

## Done Criteria

- solution builds successfully
- the first document hosts a live `DagEdit` editor control
- text-based diagnostic documents still work for the other tabs
- roadmap and screenshot notes are updated in English and Korean

## Status

Completed.
